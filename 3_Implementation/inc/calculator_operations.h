/** 
* @file studentrecord.h
* Calculator application with 4 mathematical operations
*
*/
#ifndef __STUDENTRECORD_H__
#define __STUDENTRECORD_H__

#include <stdio.h>
#include <stdlib.h>
//#include <stdio_ext.h>

/**
*  adds the operand1 and operand2 and returns the result
* @param[in] operand1 
* @return Result of operand1 + operand2
*/
int is_Exists(const char *operand1);

/**
*  subtracts the operand1, operand2 and returns the result
* @param[in] operand1 
* @return Result of operand1 - operand2
*/
void printMessageCenter(const char* operand1);

/**
*  multiply the operand1, operand2 and returns the result
 @param[in] operand1 
* @return Result of operand1 * operand2
*/
void headMessage(const char *operand1);


/**
* divides the operand1 by operand2 and returns the result 
* 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void welmes();/**
* divides the operand1 by operand2 and returns the result 
* @param[in] operand1  
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
int isnameValid(const char *operand1);/**
* divides the operand1 by operand2 and returns the result 
* @param[in] operand1 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
int IsLeapYear(int operand1);/**
* divides the operand1 by operand2 and returns the result 
* @param[in] operand1  
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
int isValidDate(Date *operand1);/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void add_student();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void initially();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void find_student();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void view();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void delete();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void cred_upd(void);/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void menu();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
void log_in();/**
* divides the operand1 by operand2 and returns the result 
* @return integer value of the operand1 / operand2
* @note returns 0 for divide by 0 error
*/
int main();

#endif  /* #define __CALCULATOR_OPERATIONS_H__ *//**
